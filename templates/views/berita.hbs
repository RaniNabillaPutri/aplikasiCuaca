<!DOCTYPE html>
<html>

<head>
    <title>Halaman Berita</title>
    <link rel="icon" href="/img/cuaca.png">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="main-content">
        {{>header}}
        <h2>{{judul}}</h2>
        <div class="isian-bawah">
            <div class="biodata-pendidikan">
                <div class="berita-list">
                    {{#each berita}}
                    <div class="berita-item">
                        <h3>{{title}}</h3>
                        <p>{{description}}</p>
                        <img src="{{image}}" alt="" srcset="" class="centered-image" style="width: 280px; height: 180px;" />
                        <br>
                        <a href="{{url}}" target="_blank">Baca selengkapnya</a>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
    {{>footer}}
</body>

</html>